# Example environment configuration for development
# Copy to .env.development and modify as needed

# Agent server connection (development only)
VITE_AGENT_HOST=localhost
VITE_AGENT_PORT=8000

# In production, the client uses window.location to connect
# to the same server that served the static files